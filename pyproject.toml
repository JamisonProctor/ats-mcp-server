[project]
name = "ats-mcp-server"
version = "0.1.0"
description = "MCP server for ATS resume evaluation using local LLM via Ollama"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
    "pymupdf>=1.24.0",
]

[project.scripts]
ats-mcp-server = "ats_mcp_server:main"
